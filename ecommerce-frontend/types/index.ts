

// the CartItem type
export type CartItem = {
    id: number;
    name: string;
    price: number;
    quantity: number;
  };
  
  // User type as an example for user context or authentication
  export type User = {
    username: string;
    email: string;
    
  };
  
 
  
  // Product type
  export type Product = {
    id: number;
    name: string;
    description: string;
    price: number;
    stock: number;
  };
  

  